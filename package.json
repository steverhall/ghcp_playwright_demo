{
  "name": "ghcp-playwright-demo",
  "version": "1.0.0",
  "description": "A simple SPA to-do list application for Playwright testing demo",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server -p 3000 -o",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "keywords": ["todo", "spa", "playwright", "demo"],
  "author": "GitHub Copilot",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "http-server": "^14.1.1"
  }
}